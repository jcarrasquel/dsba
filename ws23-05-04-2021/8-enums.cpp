#include <iostream>

enum weekday{monday,tuesday,wednesday,thursday,friday,saturday,sunday};

// week have "symbolic constants" with (default) values 0-6 (enumerators)

int main()
{
	std::string days = {"monday"}

	for(int i = monday; i <= sunday; i++)
	{
		std :: cout << days[monday] << std :: endl;
	}

	return 0;
}